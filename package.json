{
  "name": "chroma-key",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build --base=chroma-key",
    "preview": "vite preview"
  },
  "devDependencies": {
    "sass": "^1.49.11",
    "typescript": "^4.5.4",
    "vite": "^2.9.0"
  },
  "dependencies": {
    "@guiseek/typed-selector": "^1.0.1"
  }
}
